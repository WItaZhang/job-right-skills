# career-scout：访问待核实的历史候选

[上游地址](https://github.com/owieschon/career-scout) · 暂缓采纳

核实日期：2026-09-22（America/Los_Angeles）。本次 GitHub API 查询仓库及 HEAD 返回 404；直接读取 main/README 也失败。
网页搜索和网页阅读工具仍返回该地址的缓存内容，**缓存可读与当前 API 不可读不一致**。
无法判断是删除、改名、转私有还是访问问题；默认分支、当前提交和许可均为 unknown。

## 可借鉴机制

仅作后续核实线索：缓存 README 描述确定性地点/出差门槛先于 LLM 判断、对部分被拒项做有界复查、使用 decision log 解释设计取舍。

## 不应照搬之处

不能把缓存描述当作当前实现或运行证据；不能确认许可证就沿用代码；也不能把上游人物、地点、出差阈值当成本用户条件。该条不计入已核实开源项目。

## 对 job-right-skills 的映射

候选映射为 hard-constraint gate、误拒复查与决策记录，均待恢复一手访问后再评估。当前三态硬条件设计主要依据已读的 [Job Search](job-search.md)，不依赖本条缓存。

结论置信度：低；仅历史发现线索。没有 clone、安装、运行或复制该项目。
